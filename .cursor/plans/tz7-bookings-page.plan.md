# ТЗ №7 — Страница «Бронирования»

## Цель
Сделать раздел /bookings читаемым, без засветов, с карточками, статусами, пустым состоянием, mobile-first.

## Статус: ✅ выполнено

### Сделано

1. **Структура страницы**
   - Заголовок «Бронирования», список карточек или пустое состояние.

2. **Пустое состояние** (`components/booking/EmptyState.tsx` → `BookingsEmptyState`)
   - Текст: «У вас пока нет бронирований», «Когда появятся — они будут здесь».
   - Кнопка «Перейти к объявлениям» (href="/listings").
   - Центр, иконка (📅), цвета: title `var(--text-primary)`, текст `var(--text-secondary)`.

3. **Карточка бронирования** (`components/booking/BookingCard.tsx`)
   - Фото 88×88, radius 12; название квартиры; дата (checkIn — checkOut); статус; кнопки «Открыть», «Отменить» (если pending).
   - Верстка: `bg-[var(--bg-card)]`, `border border-[var(--border-main)]`, radius 16, padding 12.
   - Текст: название `var(--text-primary)`, дата `var(--text-secondary)`.
   - Mobile: кнопки full width (w-full sm:w-auto).

4. **Статусы** (`components/booking/BookingStatus.tsx`)
   - Ожидание (оранж), Подтверждено (зелёный), Отменено (красный).
   - Маппинг API: PENDING → pending, CONFIRMED → approved, CANCELED → cancelled.

5. **Действия**
   - «Открыть» — ссылка на `/listings/:listingId`.
   - «Отменить» — только для pending, с подтверждением, вызов `POST /api/bookings/:id/cancel`.

6. **Устранение засветов**
   - Все тексты через `var(--text-primary)` / `var(--text-secondary)`; без text-white и лишней opacity.

7. **API**
   - Backend: добавлен `GET /bookings` — список бронирований текущего пользователя (guestId), с listing (title, первое фото).
   - Frontend: `useFetch(['bookings-my'], '/api/bookings')`, отображение списка или пустого состояния.

8. **Mobile**
   - Карточки full width, gap 12 (space-y-3). Кнопки в карточке: на мобильном width 100%.

### Критерии готовности (ТЗ №7)
- ✔ Нет засветов
- ✔ Карточки читаемы
- ✔ Статусы есть
- ✔ Пустое состояние есть
- ✔ Mobile в порядке

### Не трогали
- Объявления, AI, header.

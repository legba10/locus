# ТЗ 34 — Полная перестройка архитектуры профиля и навигации (без дублей)

## Статус: выполнено

## Что сделано

1. **Убраны дубли глобальных меню**
   - Удален `AppSidebarTZ14` из корневого layout.
   - Удалено выпадающее меню аватарки в header.
   - Аватарка теперь ведет напрямую в `/profile`.
   - Глобальная навигация остается через нижнее меню.

2. **Новая логика профиля (центр управления)**
   - `/profile` перестроен под ролевую логику:
     - обычный пользователь: Избранное, Бронирования, Настройки;
     - арендодатель: Мои объявления, Бронирования, Календарь, Продвижение, Финансы;
     - админ: Модерация, Пользователи, Жалобы.
   - Добавлен блок **Быстрые действия** (2x2).
   - Блок **Система**: Настройки, Поддержка, Выход.

3. **Линейная навигация разделов профиля**
   - Реализован маршрут `/profile/calendar` (новая страница).
   - Удален redirect `/profile/calendar -> /profile/bookings` из `next.config.js`.
   - Подстраницы профиля работают как отдельные экраны без вложенных меню.

4. **Мои объявления**
   - На `/profile/listings` добавлены явные действия на карточках:
     - Редактировать
     - Продвижение
     - Календарь
     - Аналитика

5. **Страница объявления — роль и статусы**
   - Для админа добавлены действия:
     - Одобрить
     - Отклонить
     - Редактировать
   - Исправлен баг статусов:
     - `pending/on_review/moderation` — режим модерации,
     - `published` — «Опубликовано» (без «На модерации»),
     - `rejected` — «Отклонено».

## Измененные файлы

- `frontend/components/layout/Header.tsx`
- `frontend/app/layout.tsx`
- `frontend/app/profile/page.tsx`
- `frontend/app/profile/calendar/page.tsx`
- `frontend/app/profile/listings/page.tsx`
- `frontend/domains/listing/ListingPageTZ8.tsx`
- `frontend/next.config.js`

# ТЗ-28 — Полная переработка страницы объявления (Mobile UX + нижняя панель)

**Статус:** выполнен  
**Область:** только страница объявления (ListingLayout + StickyActions + Gallery + AIMetrics).

## Сделано

### 1. Конфликт нижнего меню
- Глобальное нижнее меню уже скрывается на `/listings/[id]` в `BottomNavGlobal.tsx` (`isListingDetail` → `return null`). Дополнительная логика не требовалась.

### 2. Новая нижняя панель объявления (StickyActions)
- Структура: **| ♡ | Написать | Забронировать |** (без блока с ценой справа на mobile).
- Высота: `min-h-[64px]`, отступ снизу `pb-[max(12px,env(safe-area-inset-bottom))]` для safe-area (iPhone).
- Скругление: `rounded-t-[16px]`.
- Тень: `shadow-[0_-4px_24px_rgba(0,0,0,0.12)]`.
- Фон: `bg-[var(--bg-card)]`, граница сверху.

### 3. Отступы (ListingLayout)
- После фото: **12px** (`mb-3`).
- Заголовок (h1) сразу под галереей.
- Город + рейтинг: компактная строка с **6px** визуально за счёт общего `space-y-4`.
- Цена и краткие характеристики — без лишних больших блоков.
- Описание: блок с отступами **16px** по сетке (`space-y-4` / `space-y-5`), без пустых зон > 24px.
- Контейнер страницы: `pb-[88px]` на mobile под sticky-панель.

### 4. Карта
- Удалён большой блок карты.
- Вместо него одна строка под городом: **📍 Город · Показать на карте** (кнопка).
- По нажатию открывается **модал карты**: высота 70–75vh, крестик закрытия, iframe Yandex.

### 5. Порядок блоков
1. Фото (галерея)  
2. Заголовок  
3. Город + рейтинг  
4. Цена (+ AI-подход при наличии)  
5. Краткие характеристики  
6. Строка «Показать на карте»  
7. Описание  
8. Удобства  
9. AI-оценка  
10. Владелец  
11. Бронирование (mobile)  
12. Отзывы  
13. Похожие объявления  

### 6. AI-оценка (AIMetrics)
- Общий индекс выведен крупно (28px).
- Категории компактно, отступы между строками до ~12px (`space-y-3`, `gap-1`).
- Заголовок «AI-оценка», меньше вертикальных отступов.

### 7. Фото-блок (Gallery)
- Свайп уже был.
- Индикатор **1/N** сохранён.
- Кнопки поверх: назад, избранное.
- Добавлена кнопка **«Все фото»** рядом с индикатором (открывает fullscreen-галерею).

### 8. Логика кнопок
- **Написать** → `onWrite()` (чат).
- **Забронировать** → `onBook()` (модал бронирования).
- **Избранное** → `onSave` (toggle), иконка с заливкой при `isSaved`.

### 9. Удалено
- Дубли кнопок на mobile (цена убрана из sticky-панели).
- Большой блок карты.
- Лишние вертикальные отступы (> 24px).

### 10. Safe-area
- Sticky-панель: `pb-[max(12px,env(safe-area-inset-bottom))]`.

## Примечание

Изменения применены к ветке с **ListingLayout** (ListingPageV3 / ListingPageLight). Если в приложении используется **ListingPageTZ8** (useNewListingUI), там свой layout и sticky-панель — при необходимости те же принципы ТЗ-28 можно перенести и туда.

# ТЗ-4: Переработка карточек объявлений (главная + список + превью)

**Цель:** карточка продаёт объявление за 1–2 секунды: бейджи, район, метрики, AI-пояснение, одна кнопка.

## Статус: ✅ Выполнено

## Что сделано

### 1. Структура карточки (ТЗ-4)

- **Фото:** слева сверху бейдж («Проверено» / «Подобрано AI»), справа избранное ♡, снизу 📍 район и 🚇 метро (если есть).
- **Бейджи под фото:** макс 2 из списка — Проверено LOCUS, Подобрано AI, Топ вариант, Новое, Скидка, Редкое (фиолетовые/синие).
- **Цена:** главный акцент, формат «3 000 ₽ / ночь» или «65 000 ₽ / мес».
- **Тип аренды:** под ценой — Посуточно / Долгосрочно / Комната / Студия.
- **Локация:** город • район (например Москва • Таганская).
- **Метрики:** одна строка — комнаты · м² · гостей · этаж.
- **AI-пояснение:** одна строка «AI рекомендует: …» из reasons/verdict.
- **Кнопка:** одна «Смотреть» внизу карточки.

### 2. Стили и UX

- Тень: `0 10px 30px rgba(0,0,0,0.08)`; при hover — лёгкое увеличение + усиленная тень.
- Отступы между карточками: 16px (мобильные), 24px на desktop (класс `listing-grid-tz4`).
- Скелетон загрузки приведён к структуре карточки (фото, бейджи, цена, локация, метрики, AI, кнопка) — без дёргания.
- Состояние «нет фото»: плейсхолдер с текстом «Нет фото».

### 3. Данные и места использования

- **HomePageV6:** маппинг дополнен — metro, rentalType, guests, badges (из isVerified, aiRecommended, score≥75, isNew), aiReasons из reasons/verdict. Во все три секции (актуальные, рекомендации AI, просмотренные) передаются новые пропсы.
- **SearchPageV4:** такой же маппинг и передача пропсов в ListingCard; сортировка по популярности исправлена для aiScore в виде объекта.

### 4. Что не трогали

- Логотип и базовая сетка сайта не менялись.
- Карточка по-прежнему ведёт на `/listings/[id]` по клику; кнопка «Смотреть» внутри карточки визуальная (весь блок — Link).

## Файлы

- `frontend/components/listing/ListingCard.tsx` — новая структура, пропсы (badges, metro, rentalType, rooms, area, guests, floor, totalFloors, aiReasons), скелетон ТЗ-4.
- `frontend/styles/listing-card-tz4.css` — стили ТЗ-4 (бейджи на фото, подвал фото, блок инфо, кнопка, скелетон, сетка).
- `frontend/styles/globals.css` — импорт `listing-card-tz4.css`.
- `frontend/app/HomePageV6.tsx` — маппинг badges, metro, rentalType, guests, aiReasons; передача всех новых пропсов в ListingCard; класс `listing-grid-tz4`.
- `frontend/app/listings/SearchPageV4.tsx` — маппинг и передача новых пропсов; сортировка popular по числовому score.
- `frontend/components/listing/index.ts` — экспорт типа `ListingCardBadge`.

## Критерии готовности (ТЗ)

- [x] Карточка информативная (цена, тип, локация, метрики, AI).
- [x] Есть бейджи (макс 2, фиолетовые/синие).
- [x] Есть район (город • район на фото и в блоке).
- [x] Есть AI-пояснение (одна строка).
- [x] Есть метрики (комнаты, м², гости, этаж).
- [x] Карточка «продаёт» (акцент на цене, кнопка «Смотреть»).
- [x] Скелетон стабильный; тени и отступы 16–24px; hover на ПК.

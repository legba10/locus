## Plan: Theme Toggle UI (TZ-2)

## Status: completed

### Scope

Add a visible theme toggle button into existing header/mobile menu without changing layout architecture.

### Tasks

1. [completed] Update `ThemeToggle` to TZ-2 contract (`ThemeContext`, `.theme-toggle` class).
2. [completed] Place toggle in desktop header right area.
3. [completed] Add mobile fallback toggle inside burger menu.
4. [completed] Add `.theme-toggle` styles and body transition in `globals.css`.
5. [completed] Lint check changed files.

# ТЗ №12 — Финальная полировка UI (перед MVP)

## Цель
Привести интерфейс к единому виду: без визуальных багов и засветов, одинаковые отступы, единые кнопки и инпуты, стабильный mobile-first UX.

## Блоки

### 1. Отступы и сетка
- Горизонтальный padding: 16px
- Gap между блоками: 16px
- Gap внутри карточек: 12px
- Радиусы: 16–20px
- Страницы: главная, поиск, объявление, мои объявления, бронирования, кабинет

### 2. Кнопки (Button)
- Типы: primary (фиолет), secondary (outline), ghost (текст)
- height: 48px, radius: 14px, font-weight: 600
- Запрет: разные кнопки на разных страницах

### 3. Инпуты
- height: 48px, radius: 12px
- border: var(--border-main), background: var(--bg-input), color: var(--text-primary)

### 4. Заголовки
- h1: 24px, h2: 20px, p: 15px, small: 13px
- Цвета: var(--text-primary), var(--text-secondary)

### 5. Проверка экранов (ручная)
Авторизация, онбординг, главная, поиск, объявление, кабинет, размещение.

### 6. Dark theme
- Нет белых текстов, нет opacity для текста
- Карточки и инпуты читаемы

### 7. Header
- Иконки ровно, аватар 36px, кнопки 44px, gap 8px

### 8. Loading
- Skeleton с shimmer: список объявлений, карточка, страница объявления

### 9. Пустые состояния
- Везде: иконка + текст + кнопка действия

### 10. Scroll
- Нет скачков, sticky кнопки, safe-area iPhone

### 11–12. Mobile и багфиксы
- Проверка Safari/Chrome на iPhone
- Исправить налезание текста, двойные padding, разные шрифты, резкие цвета

## Критерии готовности
- Выглядит как продукт, всё ровно, нет засветов, всё читаемо

---

## Выполнено (реализация)

### Блок 2 — Кнопки
- `buttons-tz5.css`: radius 14px для всех .btn, primary — var(--accent-gradient) и var(--button-primary-text), secondary — outline (transparent + border var(--border-main), hover var(--bg-input)), без rgba в dark.

### Блок 3 — Инпуты
- `components/ui/Input.tsx`: height 48 (h-12), radius 12px, border var(--border-main), bg var(--bg-input), color var(--text-primary), placeholder var(--text-muted), label 13px var(--text-secondary).

### Блок 4 — Заголовки
- `styles/tz12-typography.css`: h1 24px, h2 20px, .text-p 15px, .text-small-tz12/small 13px, цвета var(--text-primary)/var(--text-secondary). Подключён после typography.css.

### Блок 1 — Отступы и сетка
- `styles/variables.css`: --page-px: 16px, --section-gap: 16px, --card-gap: 12px, --radius-card: 18px, --radius-card-lg: 20px.
- `styles/tz12-layout.css`: .page-container-tz12, .section-gap-tz12, .card-gap-tz12, .main-with-header padding-bottom env(safe-area-inset-bottom).

### Блок 7 — Header
- Уже по ТЗ-2: header-actions gap 8px, IconButton 44×44, UserAvatar 36×36. Изменений не вносилось.

### Блок 8 — Loading
- `styles/tz12-skeleton.css`: @keyframes tz12-shimmer, класс .skeleton-shimmer-tz12 (простой градиентный shimmer).
- `ListingCard.tsx`: ListingCardSkeleton переведён на skeleton-shimmer-tz12 вместо animate-pulse.
- `ListingPageV3.tsx`: скелетон страницы объявления переведён на skeleton-shimmer-tz12, структура под layout TZ-10 (280px галерея, полоски контента).

### Блок 9 — Пустые состояния
- `ListingsGridTZ11.tsx`: пустое состояние — иконка (🔍 в круге), заголовок «Ничего не найдено», подпись, опциональная кнопка (onEmptyAction, emptyActionLabel). В SearchPageTZ11 передаётся onEmptyAction (сброс фильтров + query), label «Сбросить фильтры».
- BookingsEmptyState уже имеет иконку + текст + кнопку (без изменений).

### Блок 6 — Dark theme
- Кнопки и инпуты используют только CSS-переменные (--text-primary, --border-main, --bg-input, --accent и т.д.), засветов не добавлено.

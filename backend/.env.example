# ===========================================
# DATABASE (Neon PostgreSQL)
# ===========================================
# Get your connection string from https://neon.tech
DATABASE_URL=postgresql://username:password@ep-xxx.neon.tech/neondb?sslmode=require

# ===========================================
# FILE STORAGE (Supabase)
# ===========================================
# Supabase Project URL (same as NEXT_PUBLIC_SUPABASE_URL in frontend)
SUPABASE_URL=https://xxx.supabase.co
# Supabase Service Role Key (for backend full access)
SUPABASE_SERVICE_ROLE_KEY=eyJ...
# Bucket name for listing photos
SUPABASE_BUCKET_PHOTOS=locus-listings

# ===========================================
# AUTHENTICATION (Supabase only)
# ===========================================
# Backend accepts only Supabase access_token via Authorization: Bearer.
# No JWT — use SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY above.

# ===========================================
# AI CONFIGURATION
# ===========================================
# Options: "deterministic" (default) | "openai"
AI_PROVIDER=deterministic
# OPENAI_API_KEY=sk-...

# ===========================================
# APPLICATION
# ===========================================
NODE_ENV=development
PORT=4000

# Frontend URL (for CORS and redirects)
FRONTEND_URL=https://locus-i4o2.vercel.app

# CORS (comma-separated origins, no spaces)
CORS_ORIGINS=http://localhost:3000,https://locus-i4o2.vercel.app

# Optional: auth and Telegram (auth works without Telegram)
REAL_AUTH_ENABLED=false
TELEGRAM_ENABLED=false

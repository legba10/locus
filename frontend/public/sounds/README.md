# Sounds

Centralized sound assets:

- `message.mp3` — new message
- `booking.mp3` — new booking
- `success.mp3` — successful actions
- `error.mp3` — errors/rejections

If `.ogg` versions are available, place them next to `.mp3` with the same names.
